const circle =any.createSVG({
  type: 'circle',
  class: 'c-circle',
  attrs: {
    id:'myCircle',
    cx:'25',
    cy:'25',
    r:'20',}
});
svg.appendChild(circle);

// function renderLogo(data){
//     return
//  ${renderLogoShape(data.shape)}
// }

// module.exports = shapes
